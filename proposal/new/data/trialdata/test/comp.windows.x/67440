 from kelsey mdd comm mot com joe kelsey subject re xv three zero zero has escaped organization motorola mobile data division seattle wa lines five three in lindholm ucs ubc ca george lindholm writes in article lindholm ucs ubc ca george lindholm writes in article bradley grip cis upenn edu john bradley writes and here is the first patch the following fixes some problems with the imakefile files and makes it possible to compile xv three zero zero under solaris without having to use the ucb stuff oops i didn t get the signal processing right thats what i get from doing the work on an ascii terminal here is the fixed patch the patches for xv h need to use ifdef svr four rather than the overly complex if defined sun defined sysv in order to generically compile xv on i three eight six svr four architecture also rand and srand don t work very well use lrand four eight and seed four eight to get better results otherwise xv three zero zero compiles just fine on my esix system v four zero four box with xfree eight six and gcc two three three i just uncommented the gcc definition in the makefile added l usr x three eight six lib i usr x three eight six include to the copts line and modified libs to include lsocket lnsl really john if you just use the imakefile things like this don t even have to get mentioned one one one nine nine three zero four two eight zero eight three three one three xv h one nine nine three zero four two eight one seven four seven three eight two eight four two nine two two eight four two nine eight signal macros ifdef svr four define hold sig sighold sigalrm define release sig sigrelse sigalrm define pause sig sigpause sigalrm else define hold sig sigblock sigmask sigalrm define release sig sigblock zero define pause sig sigpause zero endif default for most folks three one eight three two three three two four three three three define random rand define srandom srand endif ifdef svr four define random lrand four eight define srandom seed four eight endif