 from car access digex net mr blue subject some rookie questions organization express access online communications greenbelt md usa lines two seven nntp posting host access digex net im new to xlib programming im having a problem i hope someone here can help me with i create a window and i want it to have some background color say red and draw an oval in it further when i resize the window i want to be able to redraw the image so that is fills the new window whether it is larger or smaller im using aixwindows on an rs six zero zero zero my problem is this when i resize the window only the original part of the window that was part of the window creation gets drawn into everything else is a grey the oval tries to redraw itself but gets clipped i can see that it would be the right size if i could see all of it but it only shows up in the region that was the original size of the window when created for example i start out the window at two zero zero x two zero zero in width and height if i resize it to four zero zero x four zero zero the only part that has any color drawn into it is is from the origin out to two zero zero x two zero zero the rest is grey further the oval only shows up in that two zero zero x two zero zero area what am i doing wrong how can i get a window to resize and redraw a re scaled image in it to take up the full size of the window second question could someone tell me what xaw and xmu are thanks chris