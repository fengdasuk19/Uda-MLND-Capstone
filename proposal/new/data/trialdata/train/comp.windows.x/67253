 from barnett grymoire crd ge com bruce barnett subject re title for xterm in reply to lanzo tekelec com s message of thu two two apr one nine nine three one seven three five four six gmt nntp posting host grymoire crd ge com reply to barnett crdgw one ge com organization ge corp r d schenectady ny in article lanzo tekelec com mark lanzo writes hint for sun os users use usr five bin echo instead of bin echo or csh s built in echo otherwise you ll have to embed literal esc and bel characters in the string instead of using convenient octal sequences bad idea using usr five bin echo is slower than the built in echo this is what i do it does execute hostname once per shell window and does read in one extra file but manipluating the titles does not require executing extra programs oh yes it does execute some programs once per each system but it stores them in a file for the next time if hostname then setenv hostname hostname endif figure how how to generate escape bell and echo commands without a a line terminator i may have done this before if so the variable e is set have i executed this script before on this system if e then echo already set the echo variables dev tty else if f echo then source echo else if echo n wc l zero then echo built in echo is bsd dev tty then berkeley style echo echo set ech echo n echo echo set e echo a tr a zero three three echo echo set b echo a tr a zero zero seven echo echo set n echo source echo else echo built in echo is sysv dev tty echo set ech echo echo echo set e zero three three echo echo set b zero zero seven echo echo set n c echo source echo endif are we using shelltool cmdtool or xterm duplicate these aliases here to avoid problems if term sun then sun aliases alias header l one alias iheader l one else if term xterm then alias header two one alias iheader one one endif bruce barnett uunet crdgw one barnett